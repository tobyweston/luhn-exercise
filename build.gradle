defaultTasks 'clean', 'build'

apply plugin: 'java'

version = '1.0.0'
group = 'org.acmebank.interviews.luhn'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}
