defaultTasks 'clean', 'build'

apply plugin: 'scala' // scala/java

version = '1.0.0'
group = 'org.acmebank.interviews.luhn'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.scala-lang:scala-library:2.12.4'
    testCompile 'junit:junit:4.12'
}
